pluginManagement {
    plugins {
        id "net.ltgt.apt-idea" version "0.21"
    }
}

include 'commons'
include 'producer'
include 'consumer'

rootProject.children.each {
    it.buildFileName = "${it.name}.gradle"
}
